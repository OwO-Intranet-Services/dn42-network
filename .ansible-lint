---
exclude_paths:
  - .ansible/
  - .vscode/

enable_list:
  - no-handler
  - no-log-password

skip_list:
  - ignore-errors
  - yaml[line-length]
  - command-instead-of-shell

warn_list:
  - experimental
  - role-name

use_default_rules: true
parseable: true
