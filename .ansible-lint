---
exclude_paths:
  - .ansible/
  - .artifacts/
  - .vscode/

enable_list:
  - no-handler
  - no-log-password

skip_list:
  - ignore-errors
  - role-name
  - yaml[line-length]
  - command-instead-of-shell

warn_list:
  - experimental

use_default_rules: true
