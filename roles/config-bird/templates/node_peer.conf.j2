{%- set peer_vars = hostvars.get(peer, {}) -%}
# /etc/bird/peers/{{ node_prefix }}{{ peer | replace('-', '_') }}.conf
# Generated by Ansible

protocol bgp {{ node_prefix }}{{ peer | replace('-', '_') }} from internal_bgp {
    neighbor {{ peer_vars.ownip6 }} as OWNAS;
}
