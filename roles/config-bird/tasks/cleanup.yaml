---
- name: Gather node list for cleanup
  ansible.builtin.set_fact:
    node_hosts: "{{ groups.get(node_group, []) | list }}"

- name: Gather dn42 node list for cleanup
  ansible.builtin.set_fact:
    dn42_hosts: "{{ groups.get(dn42_group, []) | list }}"

- name: Stop BIRD service
  ansible.builtin.systemd:
    name: bird
    state: stopped
  ignore_errors: true

- name: Disable BIRD service
  ansible.builtin.systemd:
    name: bird
    enabled: false
  ignore_errors: true

- name: Discover per-node BIRD configurations
  ansible.builtin.find:
    paths: /etc/bird/nodes
    patterns:
      - "{{ node_prefix }}*.conf"
      - "{{ node_rr_prefix }}*.conf"
    file_type: file
  register: _bird_node_cleanup_configs

- name: Remove per-node BIRD configurations
  ansible.builtin.file:
    path: "{{ item }}"
    state: absent
  loop: "{{ _bird_node_cleanup_configs.files | map(attribute='path') | list }}"
  loop_control:
    label: "{{ item | basename }}"

- name: Remove DN42 peer BIRD configurations
  ansible.builtin.file:
    path: "/etc/bird/peers/{{ dn42_prefix }}{{ (item.bgp.asn | string)[-4:] }}.conf"
    state: absent
  loop: "{{ hostvars[inventory_hostname].peers | default([]) }}"
  loop_control:
    label: "{{ item.bgp.asn }}"
  when: hostvars[inventory_hostname].peers is defined

- name: Remove BIRD main configuration files
  ansible.builtin.file:
    path: "{{ item }}"
    state: absent
  loop:
    - /etc/bird/bird.conf
    - /etc/bird/node.conf
    - /etc/bird/babel.conf
  loop_control:
    label: "{{ item }}"

- name: Remove DN42 dummy interface
  ansible.builtin.shell: |
    ifdown dn42-dummy || true
  args:
    executable: /bin/bash
  ignore_errors: true
  changed_when: false

- name: Remove DN42 interface configuration
  ansible.builtin.file:
    path: /etc/network/interfaces.d/60-dn42-dummy
    state: absent

- name: Remove DN42 sysctl configuration
  ansible.builtin.file:
    path: /etc/sysctl.d/60-dn42.conf
    state: absent

- name: Remove ROA update cron job
  ansible.builtin.cron:
    name: "Update DN42 ROAs"
    state: absent
    user: bird
