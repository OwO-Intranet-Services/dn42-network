# Managed by Ansible
{% set effective_env = flapalerted_env | combine(flapalerted_env_overrides | default({}), recursive=True) %}
{% for key, value in effective_env | dictsort %}
{{ key }}={{ value | string | quote }}
{% endfor %}