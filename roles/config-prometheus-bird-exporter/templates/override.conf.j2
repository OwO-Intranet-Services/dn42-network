# Generated by Ansible
[Service]
ExecStart=
ExecStart=/usr/bin/prometheus-bird-exporter \
    -bird.v2 \
    -bird.socket={{ prometheus_bird_exporter_socket }} \
    -web.listen-address={{ prometheus_bird_exporter_listen }} \
    -format.new \
    -proto.bgp \
    -proto.babel
