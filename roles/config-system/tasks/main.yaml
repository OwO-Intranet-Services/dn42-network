---
- name: Install system bootstrap packages
  ansible.builtin.include_tasks:
    file: install.yaml

- name: Configure system bootstrap state
  ansible.builtin.include_tasks:
    file: configure.yaml
